# @zoom/meetingsdk@6.x declares a React 18 peer while this project runs React 19.
# The Meeting SDK's embedded (Component View) is framework-agnostic and works on
# React 19, so we accept the peer mismatch. This flag makes Vercel's build install
# the same way local dev does (npm install --legacy-peer-deps).
legacy-peer-deps=true
