diff --git a/src/components/AdminButtonHealthPanel.tsx b/src/components/AdminButtonHealthPanel.tsx
index 33d6d31..1d4c68d 100644
--- a/src/components/AdminButtonHealthPanel.tsx
+++ b/src/components/AdminButtonHealthPanel.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { useAdminActionHealth } from "@/components/AdminActionHealthProvider";
+import { useAdminActionHealthSafe } from "@/components/AdminActionHealthProvider";
 
 function HealthRow({ label, value }: { label: string; value: string | number | null | undefined }) {
   return (
@@ -12,7 +12,7 @@ function HealthRow({ label, value }: { label: string; value: string | number | n
 }
 
 export function AdminButtonHealthPanel() {
-  const health = useAdminActionHealth();
+  const health = useAdminActionHealthSafe();
 
   return (
     <section className="mt-8 rounded-3xl border border-slate-300 bg-slate-50 p-6 shadow-sm">
diff --git a/src/components/AdminCompanyCard.tsx b/src/components/AdminCompanyCard.tsx
index 50b2f48..8dfb9d9 100644
--- a/src/components/AdminCompanyCard.tsx
+++ b/src/components/AdminCompanyCard.tsx
@@ -37,10 +37,11 @@ type Props = {
 
 function formatDate(value: string | null) {
   if (!value) return "—";
-  return new Date(value).toLocaleDateString(undefined, {
+  return new Date(value).toLocaleDateString("en-US", {
     year: "numeric",
     month: "short",
     day: "numeric",
+    timeZone: "UTC",
   });
 }
 
diff --git a/src/components/AdminDashboardShell.tsx b/src/components/AdminDashboardShell.tsx
index 391d968..c40f54e 100644
--- a/src/components/AdminDashboardShell.tsx
+++ b/src/components/AdminDashboardShell.tsx
@@ -43,54 +43,56 @@ export function AdminDashboardShell({
       userRole={userRole}
       serviceRoleConfigured={serviceRoleConfigured}
     >
-      <SectionHeader
-        eyebrow="Admin dashboard"
-        title="Review and publish curated opportunities"
-        description="Manage submitted companies, pending diligence reviews, document uploads, and approval decisions."
-      />
-
-      <AdminButtonHealthPanel />
-
-      <section className="mt-8 grid gap-4 md:grid-cols-5">
-        <MetricCard label="Founders" value={String(metrics.founders)} detail="Registered founder profiles" />
-        <MetricCard label="Companies" value={String(metrics.companies)} detail="Total company submissions" />
-        <MetricCard
-          label="Pending reviews"
-          value={String(metrics.pendingReviews)}
-          detail="Awaiting admin decision"
+      <div>
+        <SectionHeader
+          eyebrow="Admin dashboard"
+          title="Review and publish curated opportunities"
+          description="Manage submitted companies, pending diligence reviews, document uploads, and approval decisions."
         />
-        <MetricCard label="Documents" value={String(metrics.documents)} detail={`${metrics.pitchDecks} pitch decks`} />
-        <MetricCard
-          label="Published deals"
-          value={String(metrics.publishedDeals)}
-          detail="Live on marketplace"
-        />
-      </section>
 
-      {pendingCount > 0 ? (
-        <section className="mt-8 rounded-3xl border border-amber-200 bg-amber-50 p-6">
-          <h2 className="text-lg font-semibold text-amber-950">Pending review queue</h2>
-          <p className="mt-1 text-sm text-amber-900">
-            {pendingCount} {pendingCount === 1 ? "company needs" : "companies need"} a decision.
-          </p>
+        <AdminButtonHealthPanel />
+
+        <section className="mt-8 grid gap-4 md:grid-cols-5">
+          <MetricCard label="Founders" value={String(metrics.founders)} detail="Registered founder profiles" />
+          <MetricCard label="Companies" value={String(metrics.companies)} detail="Total company submissions" />
+          <MetricCard
+            label="Pending reviews"
+            value={String(metrics.pendingReviews)}
+            detail="Awaiting admin decision"
+          />
+          <MetricCard label="Documents" value={String(metrics.documents)} detail={`${metrics.pitchDecks} pitch decks`} />
+          <MetricCard
+            label="Published deals"
+            value={String(metrics.publishedDeals)}
+            detail="Live on marketplace"
+          />
         </section>
-      ) : null}
 
-      <AdminInvestorActivity
-        interests={investorActivity.interests}
-        introRequests={investorActivity.introRequests}
-        savedDeals={investorActivity.savedDeals}
-      />
+        {pendingCount > 0 ? (
+          <section className="mt-8 rounded-3xl border border-amber-200 bg-amber-50 p-6">
+            <h2 className="text-lg font-semibold text-amber-950">Pending review queue</h2>
+            <p className="mt-1 text-sm text-amber-900">
+              {pendingCount} {pendingCount === 1 ? "company needs" : "companies need"} a decision.
+            </p>
+          </section>
+        ) : null}
 
-      <section className="mt-8 grid gap-5">
-        {companyCards.length === 0 ? (
-          <div className="rounded-3xl border border-slate-200 bg-white p-8 text-center text-sm text-slate-600">
-            No companies submitted yet.
-          </div>
-        ) : (
-          companyCards.map((company) => <AdminCompanyCard key={company.id} company={company} />)
-        )}
-      </section>
+        <AdminInvestorActivity
+          interests={investorActivity.interests}
+          introRequests={investorActivity.introRequests}
+          savedDeals={investorActivity.savedDeals}
+        />
+
+        <section className="mt-8 grid gap-5">
+          {companyCards.length === 0 ? (
+            <div className="rounded-3xl border border-slate-200 bg-white p-8 text-center text-sm text-slate-600">
+              No companies submitted yet.
+            </div>
+          ) : (
+            companyCards.map((company) => <AdminCompanyCard key={company.id} company={company} />)
+          )}
+        </section>
+      </div>
     </AdminActionHealthProvider>
   );
 }
diff --git a/src/components/AdminInvestorActivity.tsx b/src/components/AdminInvestorActivity.tsx
index 677c2f5..0efb214 100644
--- a/src/components/AdminInvestorActivity.tsx
+++ b/src/components/AdminInvestorActivity.tsx
@@ -1,3 +1,5 @@
+"use client";
+
 function formatActivityRow(row: {
   id: string;
   status?: string | null;
@@ -9,7 +11,9 @@ function formatActivityRow(row: {
 }) {
   const investor = row.profiles?.full_name ?? row.profiles?.email ?? "Unknown investor";
   const company = row.companies?.company_name ?? "Unknown company";
-  const date = row.created_at ? new Date(row.created_at).toLocaleDateString() : "—";
+  const date = row.created_at
+    ? new Date(row.created_at).toLocaleDateString("en-US", { timeZone: "UTC" })
+    : "—";
 
   return { id: row.id, investor, company, status: row.status ?? "—", date, message: row.message ?? null };
 }
